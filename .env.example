# Wrangler CLI auth (deploy, migrations, `npm run tokens`). Copy to .env; never commit .env.
# Token scopes: Workers Scripts:Edit, D1:Edit, Workers Routes:Edit, Workers KV Storage:Edit
CLOUDFLARE_API_TOKEN=
CLOUDFLARE_ACCOUNT_ID=

# Used by `npm run seed` and `npm run smoke`.
HUML_URL=https://huml.ai/mcp
HUML_TOKEN=
